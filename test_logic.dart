void main() {
  List<bool> setsState1 = [true, true, true]; // Exercise 0
  List<bool> setsState2 = [true, true, true]; // Exercise 1 (already done out of order)
  List<bool> setsState3 = [false, false, false]; // Exercise 2 (needs to be done)

  int exIndex = 0; // We just completed the last set of Exercise 0
  int nextExIndex = exIndex + 1;

  print("Next is $nextExIndex");
}
